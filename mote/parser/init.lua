-- mote parsers

return {
    core = require("mote.parser.core"),
    http = require("mote.parser.http"),
    uri = require("mote.parser.uri"),
    ip = require("mote.parser.ip"),
    mime = require("mote.parser.mime"),
    multipart = require("mote.parser.multipart"),
}
